// function identifier (ParameterList opt) { FunctionBody }
// ini adalah function yang dipelajari dari awal dengan penulisan atau urutan penulisan yang normal

// contoh

function cthFunction(cthSatu, cthDua) {
  let hasil = cthSatu * cthDua;
  console.log(hasil + " adalah hasil perkalian dari " + cthSatu + " dikali " + cthDua);
  return;
}
cthFunction(20, 40);
